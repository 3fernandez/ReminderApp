<%= simple_form_for @annotation, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :body %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                annotations_path, :class => 'btn' %>
  </div>
<% end %>
